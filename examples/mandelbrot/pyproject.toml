[project]
name = "mandelbrot"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "magicbind>=0.1.0",
    "numpy",
    "pillow>=12.2.0",
]

[tool.uv.sources]
magicbind = { path = "../../", editable = true }
