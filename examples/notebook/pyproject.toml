[project]
name = "notebook-example"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "magicbind>=0.1.0",
    "jupyter",
    "matplotlib",
    "numpy",
]

[tool.uv.sources]
magicbind = { path = "../../", editable = true }
